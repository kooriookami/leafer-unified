{
  "name": "leafer-unified",
  "version": "1.0.0",
  "description": "A union entrypoint of Leafer for both browser and Node.js.",
  "scripts": {
    "build": "vite build -- browser && vite build -- node",
    "publish": "npm publish dist/ --registry https://registry.npmjs.org"
  },
  "author": "kooriookami",
  "license": "MIT",
  "devDependencies": {
    "jsonfile": "^6.2.0",
    "rollup-plugin-copy": "^3.5.0",
    "sort-package-json": "^3.4.0",
    "vite": "^7.1.12"
  }
}
